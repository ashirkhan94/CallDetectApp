/**
 * @format
 */

import { AppRegistry } from 'react-native';
import App from './App';
import CallDetectPage from './CallDetectPage';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => CallDetectPage);
